// Optimises apps/*/assets-src into public/ with sharp. Implemented in Phase 3 (BUILD_SPEC.md §6.4).
console.error(
  'pnpm assets is not available yet: it is built in Phase 3 together with the first spoke app.',
)
process.exit(1)
